# GSN — next round (Nathan's list, 2026-09-10)

Captured verbatim-in-substance before a context clear; the next session
executes against this. Order below is Nathan's.

1. **Corridor mode.** Upload a GPX, get every node within N miles of the
   route, exportable back to the device. The touring rider's killer
   feature; Nathan has a 2027 test case with a very long route.
2. **Era competence.** Tag nodes by what they can actually touch:
   loop-frame, Tonti, spine-frame, CARC, small-block, V85, V100/PADS.
   The vintage crowd is the loudest part of the Guzzi universe and
   "full-service" doesn't tell them whether the shop has ever seen a
   Bing carb.
3. **The Desert Index.** Publish support-desert metrics — miles to
   nearest full-service by state and country, the ten worst gaps in
   America. Data-journalism story (Common Tread / Motorcyclist bait);
   a neutral finding, not a review, so it fits doctrine.
4. **Garmin GPI export** alongside GPX/KML. The Zumo people will love
   you specifically.
5. **Two dates per node**: last scraped vs. last confirmed by a rider,
   with method (web / phone / visited). Freshness is shown already;
   showing *how* fresh is the trust upgrade, and it makes the scraped
   Italian nodes honest about being thin.
6. **Frictionless reports.** PRs are a high bar for a 68-year-old with
   an Eldorado. A form that prefills a GitHub issue, or even a mailto
   with a templated body — no accounts, no backend, consistent with
   PRIVACY.md.
7. **Events layer.** GMG, MGNOC rallies, regional meets as time-bounded
   nodes. A reason to come back monthly instead of once per breakdown.
8. **Public GeoJSON endpoint** with a stable schema under the ODbL
   already chosen. What lets the Discord bot, the guzzibot, and other
   people's projects exist without us doing the work.
9. **A counter sticker.** QR to the site, offered free to every
   full-service node. Dealers become distribution; it's the only
   advertising they'll ever get for nothing.

---

## Status — session of 2026-09-10 (all nine landed)

1. **Corridor mode** — DONE. Was 90% built pre-round; this session fixed the real gap:
   exports ignored the corridor (visibleGeo never called nearRoute) — a corridor rider
   got the whole database on their GPS. Also: visible ⤒ GPX import button (was a hidden
   double-click), corridor-named export files, contract test.
2. **Era competence** — DONE. `eras:` enum (loop-frame…v100-pads), build-validated,
   filterable, in exports; doctrine: evidence-named platforms only, absent = unknown.
   7 nodes seeded from existing observations; mgclassics deliberately left untagged.
3. **Desert Index** — DONE. scripts/desert_index.py, GeoNames towns ≥5k; report +
   site/desert.html. Headline: Williston ND, 550 mi to nearest full-service (161 to a
   satellite — the two-column contrast IS the story). Ready for Nathan to pitch.
4. **Garmin GPI** — DONE. Prebuilt at build via gpsbabel (CI installs it), ↓ GPI button,
   roundtrip test. Whole-db only (GPI is binary; corridor filtering stays GPX/KML).
5. **Two dates** — DONE. Derived, not stored: last_scraped vs last_rider+method,
   glyph pills (🌐/📞/👁/🛒). Database truth: 443 scraped-dated, ONE rider-confirmed
   (artmoto) — adopt-a-node now has a visible scoreboard.
6. **Frictionless reports** — DONE minus one dependency: issue links now prefill every
   form field; mailto path implemented but dark — **guzzisupport.network has no MX**.
   Nathan: one Cloudflare Email Routing address and REPORT_EMAIL lights it up.
7. **Events layer** — DONE. layer/capability `event`, dated+located, past events fade;
   seeded GMG 2026 + five MGNOC-calendar US events (checked 2026-09-10). Organizer
   personal contacts excluded by doctrine.
8. **GeoJSON endpoint** — DONE. docs/schema.md v1 stability contract; geojson stamped
   with schema_version/license/attribution; README "Use the data".
9. **Counter sticker** — DONE to Nathan's brief (QR + URL + solid abstract transverse
   V-twin, red, 100×75mm + bleed, QR machine-verified). site/sticker.svg, footer-linked.

Also this session: GuzziOracle spec + Addendum A banked at docs/research/ with triage
notes (GuzziTech-ban conflict, manual-rights posture, Mistral/EU decision recorded);
fuel-grade idea assessed and rejected → Muninn (pure-gas.org covers E0; 91+ E10 is a
non-issue).

## Post-round decisions (Nathan, 2026-09-10, same day)

- **Email: YES.** Standard address agreed in code comments: `fix@guzzisupport.network`.
  Blocked only on Cloudflare Email Routing being enabled for the domain (no MX today) —
  once MX appears, set REPORT_EMAIL in site/index.html and rebuild; translations are
  already in place.
- **Global desert heat layer: SHIPPED same day** (DESERT toggle on the map — green=good,
  red=bad, grey=desert, with the coverage-honesty rule printed on it).
- **Sticker: Naomi (Nathan's daughter) is hand-drawing the V-twin and designing the
  sticker.** The generated site/sticker.svg is a placeholder until her art arrives.
  Pipeline for her design: scan/photo → vectorize → embed the machine-verified QR
  (make_sticker.py shows the QR generation + zbar verification recipe) → print proof.
  Her art replaces mine; the QR verification step stays.
- **Basemap swap (2026-09-10):** Carto's keyless endpoint began watermarking every tile
  "API KEY REQUIRED" (verified with raw tile fetches). Moved to OSM standard tiles —
  keyless, one provider; dark mode is now a CSS filter over the same tiles, so there is
  no second tile source to break separately. sw.js tile cache repointed. $0. If OSM's
  best-effort service ever throttles us (a viral Desert Index day), the paid standby is
  Stadia Maps at ~$20/mo (within Nathan's stated budget) — decision deferred until needed.
