### New node / update / re-verification  (pick one)

- Node id:
- What is it? (dealer / indie shop / parts depot / builder / knowledge)
- Capability grade (see /doctrine.md):
- What would a stranded rider actually USE it for? (functions)
- Brands sold/serviced:
- URL + phone:
- I verified this on (date) by (visited / called / bought / website only):

> "website only" caps a new node's status at `unverified`. Your last line sets
> `last_verified` + `verified_by`.
